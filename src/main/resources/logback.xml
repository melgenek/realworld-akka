<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <logger name="slick" level="WARN"/>
    <logger name="realworld" level="DEBUG"/>

    <root level="TRACE">
        <appender-ref ref="STDOUT"/>
    </root>

</configuration>
